# Οδηγίες συντήρησης SchoolGr

1. Όλες οι περιγραφές αλλαγών, τα έγγραφα λειτουργίας και οι καταχωρίσεις
   συνεδριών γράφονται στα Ελληνικά.
2. Σε κάθε ουσιαστική συνεδρία ενημερώνεται το `docs/SESSION_LOG.md` με
   ημερομηνία, αλλαγές, ελέγχους και τυχόν εκκρεμότητες.
3. Όταν αλλάζει η υποδομή, ενημερώνονται ταυτόχρονα το `README.md` και το
   `docs/DEPLOYMENT.md`.
4. Δεν καταχωρίζονται ποτέ στη Git πραγματικά δεδομένα σχολείου, SQL dumps,
   αρχεία χρηστών, κωδικοί, tokens, ιδιωτικά κλειδιά ή SMTP στοιχεία.
5. Πριν από κάθε commit ελέγχονται το `git diff --cached` και τα αρχεία που
   πρόκειται να δημοσιευθούν.
6. Οι κωδικοί βάσης δεδομένων παρέχονται μόνο μέσω μεταβλητών περιβάλλοντος ή
   διαχειριζόμενου secret store.
7. Μετά από αλλαγές PHP εκτελείται έλεγχος σύνταξης. Μετά από αλλαγές
   υποδομής ελέγχονται HTTPS, redirect, προστατευμένες διαδρομές και υπηρεσίες.
8. Στο τέλος κάθε συνεδρίας, εφόσον οι έλεγχοι περάσουν, γίνεται commit και
   push στο αποθετήριο `Panikkos88/SchoolGr`, εκτός αν ο χρήστης ζητήσει ρητά
   διαφορετικά.
